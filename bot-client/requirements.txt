# Selenium desktop client; use a separate venv from the FastAPI app (../requirements.txt).
httpx==0.28.1
keyring>=25.0.0
keyrings.alt>=5.0.0  # headless Linux fallback (no desktop keyring daemon)
selenium>=4.27.0
webdriver-manager>=4.0.2
pandas>=2.2.0
requests>=2.32.0
psutil>=6.0.0
# OS-level window focus + Escape (Instagram account sheet when Selenium keys miss)
pyautogui>=0.9.54
PyGetWindow>=0.0.9
rich>=13.0.0
textual>=0.80.0
